# Resolve peer dependency conflicts (React 19, date-fns@4 vs older peer ranges).
# Without this, npm install fails on react-day-picker, @solana/wallet-adapter-wallets deps, etc.
legacy-peer-deps=true
