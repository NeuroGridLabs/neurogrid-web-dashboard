# NeuroGrid Protocol — Epoch 0 / Genesis
# Copy to .env.local and fill. Never commit .env or .env.local.

# ========== Treasury (Epoch 0: optional overrides; defaults = production vaults) ==========
# NEXT_PUBLIC_* are exposed to the client; use for display/balance fetch.
NEXT_PUBLIC_TREASURY_SOL=
NEXT_PUBLIC_TREASURY_BTC=
NEXT_PUBLIC_TREASURY_ETH=
NEXT_PUBLIC_TREASURY_SUI=
# Server-only fallback (API balance fetch)
TREASURY_SOL=
TREASURY_BTC=
TREASURY_ETH=
TREASURY_SUI=
# Solana vault for payments (defaults to SOL vault above)
NEXT_PUBLIC_TREASURY_WALLET=

# ========== Genesis & Nodes ==========
# Admin Wallet: owns Alpha-01 Genesis Node; Miner page shows "Genesis Command Center" when connected
NEXT_PUBLIC_ADMIN_WALLET=
NEXT_PUBLIC_GENESIS_WAITLIST_URL=https://discord.gg

# Nodes API (Epoch 0: leave unset for empty list)
NODES_API_URL=
NEXT_PUBLIC_NODES_API_URL=
TOP_EARNERS_API_URL=

# ========== Solana ==========
NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta
NEXT_PUBLIC_SOLANA_RPC=

# ========== Deploy (tunnel / backend) ==========
DEPLOY_GATEWAY_TEMPLATE=
DEPLOY_PORT=
